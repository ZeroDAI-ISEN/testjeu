using UnityEngine;

public class moncul : MonoBehaviour
{
    public int macul;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log(macul);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
